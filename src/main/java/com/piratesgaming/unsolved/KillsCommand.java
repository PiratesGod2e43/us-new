package com.piratesgaming.unsolved; import org.bukkit.*;import org.bukkit.command.*;import org.bukkit.entity.Player;public class KillsCommand implements CommandExecutor{final PiratesUnsolved p;public KillsCommand(PiratesUnsolved p){this.p=p;}public boolean onCommand(CommandSender s,Command c,String l,String[]a){if(!(s instanceof Player q))return true;p.ensurePlayerData(q);q.sendMessage(ChatColor.YELLOW+"Your kills: "+ChatColor.WHITE+p.kills(q));return true;}}
